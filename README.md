# smart--dump-monitored

Smart trash can with local variables for weight, volume, and coordinates. It consumes a "Weather API" and sends data to "ThinkSpeak" via MQTT. The prototype will be applied in a smart city scenario.
